import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class BottomNav extends Component {
  render() {
    return (
      // some buttons based on view:
      // 1 - chapter view: next chapter, previous chapter
      // 2 - all others: library view, search view, settings view
    );
  }
}