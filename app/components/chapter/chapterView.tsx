import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class ChapterView extends Component {
  render() {
    return (      
      // lateral scroll chapter pages. No header
    );
  }
}