import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class MangaSummary extends Component {
  render() {
    return (
      //cover image, infos and summary
    );
  }
}