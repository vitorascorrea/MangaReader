import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class MangaHeader extends Component {
  render() {
    return (
      // header with select all and sorting options if mangaChapterList tab is active; empty else
    );
  }
}