import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class SearchResultList extends Component {
  render() {
    return (
      // list of results for given search. Each links to a mangaView
    );
  }
}